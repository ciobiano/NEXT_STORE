/** @type {import('next').NextConfig} */
const nextConfig = {
	experimental: {
		appDir: true,
	},f
};

module.exports = nextConfig;

moduleexports = {
	images: {	
		domains : ['links.papareact.com','fakestoreapi.com']
	},
};