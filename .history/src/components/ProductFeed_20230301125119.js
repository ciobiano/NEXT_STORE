import React from 'react'

function ProductFeed({products}) {
  return (
    <div className=""> 
    <h1> products here</h1>
    {products.map((product) => (
        
    )}
    </div>
  )
}

export default ProductFeed