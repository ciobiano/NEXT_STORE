/** @type {import('next').NextConfig} */
const nextConfig = {
	experimental: {
		appDir: true,
	},
};

module.exports = nextConfig;

module.exports = {
	
};
// next.config.js

module.exports = {
  experimental: {
    appDir: true,
  },
};
